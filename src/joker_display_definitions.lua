if JokerDisplay then


-- JokerDisplay.Definitions["j_stanczyk_1"] = {
--     calc_function = function(card)
--         local extra = card.ability and card.ability.extra or {}

--         local sold = extra.jokers_sold or 0

--         return {
--             slots = math.min(1 + math.floor(sold / 15), 5),
--             progress = sold % 15
--         }
--     end,

--     text = {
--         {
--             text = "Slots: {slots}"
--         },
--         {
--             text = "{progress}/15 to next"
--         }
--     }
-- }


end